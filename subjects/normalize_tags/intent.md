# normalize_tags

Write a Python function `normalize_tags(tags)` that takes a list of tag values and returns a normalized list of strings.

Normalization should produce clean, consistent tags suitable for deduplication and comparison.

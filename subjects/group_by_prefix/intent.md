# group_by_prefix

Write a Python function `group_by_prefix(items: list[str], sep: str) -> dict[str, list[str]]` that groups items by their prefix.

Given a list of strings and a separator, group items by their prefix (the text before the first occurrence of `sep`). Return a dict mapping each prefix to a list of suffixes in insertion order.

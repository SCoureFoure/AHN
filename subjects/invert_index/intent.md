# invert_index

Write a Python function `invert_index(pairs: list[tuple]) -> dict` that inverts a list of key-value pairs.

Given a list of `(key, value)` tuples, return a dict mapping each value to the list of keys that produced it, in insertion order. Duplicate `(key, value)` pairs must be preserved — do not deduplicate.

# TodoList — Cycle 1: Basic list operations

Implement a `TodoList` class in a file called `solution.py`.

The class must support:

- `add(item: str) -> None` — append item to the list
- `list_items() -> list[str]` — return all items in insertion order

# TodoList — Cycle 3: Remove items

Extend the existing `TodoList` class with item removal.

Add this method:

- `remove(item: str) -> None` — remove the first matching item (done or pending). Raises `ValueError` if item not in the list.

All existing methods must continue to work correctly after removal.

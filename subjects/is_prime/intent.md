# Subject: is_prime

Write a function `is_prime(n: int) -> bool` in a Python file named `solution.py` that returns True if and only if `n` is a prime number.

Place the file at the root of your working directory. Do not add a `__main__` block. Do not print anything.
